# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: write 

jobs:
  build:

    runs-on: windows-latest

    steps:
      # Checkout code
      - name: Checkout
        uses: actions/checkout@v4

      # Setup .NET (adjust version if needed)
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      # Restore dependencies
      - name: Restore dependencies
        run: dotnet restore

      # Build & Publish as self-contained exe
      - name: Build exe
        run: dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true

      # Upload exe as an artifact
      - name: Upload exe
        uses: actions/upload-artifact@v4
        with:
          name: Tempo.Exporter
          path: |
            **/publish/*
      
      # Zip the artifacts
      - name: Zip artifacts
        run: |
          # Make sure we are in the repo root
          cd $GITHUB_WORKSPACE
          # Compress the publish folder(s)
          Compress-Archive -Path ./**/publish/* -DestinationPath Tempo.Exporter.zip

      # Create a release with the artifacts attached
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          name: "Release ${{ github.run_number }}"
          files: ./Tempo.Exporter.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
